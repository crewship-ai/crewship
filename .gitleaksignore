# Crewship .gitleaksignore
# Commit-level fingerprint allowlist for gitleaks.
# Format: <commit-sha>:<path>:<rule-id>:<line>
#
# Do NOT add entries for REAL secrets — rotate the credential first.
# Each entry below has been manually verified as a false positive.

# Scrubber source contains regex patterns to DETECT private keys.
# The regex literal itself matches gitleaks' private-key rule. Not a real key.
11003c24d54ab4bf2bccfeff49f3f9f7c32a9916:internal/scrubber/scrubber.go:private-key:30

# Wireframe HTML mockups (file already deleted on HEAD; lives only in
# history). Both are UI mockup strings, not real credentials:
#   - 07-agent-runs.html:128 shows the *display name* "ANTHROPIC_KEY_1"
#     in an error row mocking a rate-limit UI.
#   - 21-admin.html:382 hits the generic-api-key rule on the substring
#     "APIs', 'bg-neutral-100" inside a Tailwind class concatenation.
452b7e08804fc11d72e9a80311065c016fb5ac36:.claude/context/wireframes/07-agent-runs.html:generic-api-key:128
452b7e08804fc11d72e9a80311065c016fb5ac36:.claude/context/wireframes/21-admin.html:generic-api-key:382

# Redaction package test fixture — synthetic sk_live_ value used to
# exercise Secret() masking on a Stripe-shaped input. The fixture was
# already replaced with the neutral FAKEPREFIX_ prefix on HEAD (see
# commit b6cd376a). This entry exists only because gitleaks scans
# full branch history and still sees the original commit, which
# would otherwise turn CI red on every PR built on top of this branch.
# Not a real secret — Stripe key entropy is irrelevant to the test;
# the redaction logic only looks at length.
34996f8ccf97b62f182a8bb293c1b4ec44df2299:internal/cli/redact/redact_test.go:stripe-access-token:22

# Audit notes accidentally committed in 7d57b511 (kill-seed-scripts
# branch) and deleted in c0db46c4. The file was a captured snapshot
# of leaked GitHub issues; the matched substrings are per-request
# data from those issue bodies, not credentials:
#   - line 1312: a Sec-WebSocket-Key header value. RFC 6455 requires
#     the client to generate this per-handshake from random bytes,
#     so the value is not a credential even before the issue was
#     deleted.
#   - line 1316: a /ws?token=… URL value from a session log; the
#     underlying session was revoked when the source ended.
# Entries here only because gitleaks scans full branch history,
# even for files that no longer exist on HEAD.
7d57b511af600f436f2d2a9dfb0ec12c7a546bc5:audit/leaked-issues-snapshot-2026-05-23.md:generic-api-key:1312
7d57b511af600f436f2d2a9dfb0ec12c7a546bc5:audit/leaked-issues-snapshot-2026-05-23.md:generic-api-key:1316

# Mission re-attach boot test (PR #641) — fake JWT secret in test setup,
# same "test-secret-for-*-32ch" convention as the other server tests.
# generic-api-key trips on entropy; manually verified not a real credential.
1b0e41cbe8e80747f7a85f86d36da82089c107f9:internal/server/mission_reattach_boot_test.go:generic-api-key:48
# Same finding under the pre-squash branch commit (PR #641's head before
# squash-merge) — covers PR-branch scans and local full-history runs.
06c261ba16f7c9683d6b2e8fb10f4df5b73cbe0d:internal/server/mission_reattach_boot_test.go:generic-api-key:48

# Composio handler tests (PR #696) — a throwaway 32-byte AES key (hex) that
# exercises credential encryption. Already removed from HEAD (built at runtime
# via strings.Repeat in armTestEncryptionKey), but gitleaks scans full branch
# history and still sees the literal in these two commits. Not a real secret —
# the deterministic "0123456789abcdef"×4 test value.
5d53262b7201743f181b7d3bc8e22a8c2cbd20a7:internal/api/composio_handler_test.go:generic-api-key:110
5fb1efc9e308e5f1c889be57255e7fa357a3996d:internal/api/composio_handler_test.go:generic-api-key:380
5fb1efc9e308e5f1c889be57255e7fa357a3996d:internal/api/composio_handler_test.go:generic-api-key:476

# Agent-proposed credentials tests (PR #706) — a fake redaction-canary value in
# the metadata-redaction assertion (TestCreateEscalation_CredentialProposal_RedactsAndLinks).
# Renamed from a secret-keyworded const at HEAD, but gitleaks scans full branch
# history and still sees the literal in the original commit. Not a real secret —
# its whole purpose is to assert the value gets redacted from the escalation row.
242fdbffc9b50d36cbcfd5dd84b1e3e45a491ddf:internal/api/escalation_credential_test.go:generic-api-key:151
