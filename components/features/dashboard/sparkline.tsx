// Promoted to components/ui/sparkline (wave 0, #2300). Re-exported so the
// dashboard's imports keep working; new code imports from components/ui.
export { Sparkline, sparklinePoints } from "@/components/ui/sparkline"
