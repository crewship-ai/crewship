# Skip budget baseline — the maximum number of `t.Skip` / `t.Skipf` /
# `t.SkipNow` calls allowed in tracked *_test.go files.
#
# Enforced by scripts/skip-budget.sh, which runs in the `shell` job of
# .github/workflows/ci.yml. The gate fails when the count RISES above this
# number; lowering it is always allowed and the script nudges you to record
# the win here.
#
# To raise this number legitimately, see the failure message the script
# prints: tracking issue + `// SKIP-WAIVER(#NNNN):` comment above the skip +
# the bump in the same commit.
#
# History
#   119  2026-07-30  Initial measurement at 225bf153 (80 test files). Roughly:
#                    ~28 Docker-daemon guards, ~26 runtime.GOOS guards, ~9
#                    testing.Short guards, a handful for missing binaries and
#                    live-provider credentials. No waiver comments existed
#                    when the gate landed; these are grandfathered.
#   120  2026-07-30  +1 from #1546's durable-write regression guard
#                    (internal/memory/persona_writefailure_test.go). It injects
#                    failure via a read-only directory, which cannot deny root,
#                    so it guards on euid. Permanent platform guard, not debt —
#                    carries a SKIP-WAIVER comment but deliberately no tracking
#                    issue, because there is nothing to come back and fix.
#   121  2026-07-30  +1 from #1486's tamper-evidence bypass suite
#                    (internal/journal/verify_bypass_test.go). Real debt, and
#                    the only entry here that is: the test states an invariant
#                    that does NOT hold today — VerifyChain reports OK after a
#                    permanent→normal priority downgrade — and is skipped
#                    behind SKIP-WAIVER(#1572) so the fix has a red test to
#                    turn green. Lower this back to 120 when #1572 lands.
#   120  2026-07-30  −1: #1572 landed. The waived test is unskipped and green,
#                    the waiver comment is gone, and the debt entry above is
#                    closed out rather than left standing.
#   123  2026-08-07  +3 from #1836's composition substrate. None is debt; all
#                    three are conditional skips that report honestly instead
#                    of asserting over an empty set, and each carries a
#                    SKIP-WAIVER comment:
#                      · crewship_step_test.go — a save-time guard against a
#                        verb with no policy action. Every verb is governed, so
#                        there is nothing to refuse; it un-skips itself the day
#                        an ungoverned verb is declared.
#                      · crewship_actions_test.go — the dispatch-time twin of
#                        the same guard, skipping for the same reason.
#                      · crewship_step_test.go — runtime.Caller guard on the
#                        schema-parity test. Permanent platform guard, never
#                        taken on any platform CI runs.
#                    The branch also REMOVED two skips it had added earlier:
#                    chain_depth_hop_test.go's pair were marked BROKEN because
#                    the composition budget did not survive the journal hop.
#                    It does now (depth) and the chain no longer re-roots every
#                    hop (origin), so both are unskipped and green rather than
#                    left standing as documentation of a fixed defect.
#   125  2026-08-11  +2 net from the prod-upgrade findings branch, which added
#                    12 skips and keeps 2. Ten were eliminated rather than
#                    waived: the six runtime.GOOS == "windows" guards became
#                    `unix` build tags (cmd_start_socket_unix_test.go,
#                    internal/config/paths_unix_test.go,
#                    server_ipc_conn_refused_unix_test.go), the mkfifo and
#                    /dev/null guards became t.Fatalf (a unix box missing either
#                    is broken, not a reason to report ok), and the two
#                    socket-guard root guards were rewritten to reach the same
#                    branches via ENOTDIR and ELOOP, which no euid can bypass.
#                    The 2 that remain are cmd_doctor_nocreate_test.go's
#                    unwritable-data-dir pair: an unwritable directory IS their
#                    premise and root writes through mode 0500, so at euid 0
#                    they would pass while asserting the opposite. Both carry
#                    SKIP-WAIVER(#1929); walIndexUnbuildable, the decision they
#                    drive, now has a permission-free direct test that does run
#                    as root.
#
# 2026-08-18  +8      The chat-surface branch (#1975). Seven guard a chmod 0555
#                    that root ignores, so as uid 0 they would pass while
#                    proving nothing; they exist because the container-write
#                    fallback hangs on errors.Is(err, fs.ErrPermission) for the
#                    error localfs really returns on EACCES, and until this
#                    branch nothing tested that boolean. Two of the defects the
#                    branch fixes were invisible for exactly that reason. The
#                    eighth runs the real container delete script through the
#                    host shell — the only way to catch `rm -f` refusing a
#                    directory, which a mock exec reports as success. All eight
#                    carry SKIP-WAIVER(#1977).
#
# 2026-08-19 (#1935): 125 -> 126. TestPageDocument_Validate_RejectsReservedSchema
#                     searches the schema vocabulary for one that is Known but
#                     not Producible, and skips when the set is empty. It is
#                     empty until embed.v1 lands: embed is Known always and
#                     Producible only where an operator has vetted an allow-list,
#                     so it is the reserved case the test wants. Carries
#                     SKIP-WAIVER(#1935). The skip stops firing once embed.v1 is
#                     on the branch; lowering this back to 125 is the win to
#                     record when the whole epic is on main.
#
# 2026-08-19          Merge of the two lines above: the pages epic's +1 and the
#                     chat-surface branch's +8 land together, so the baseline is
#                     125 + 8 + 1. Neither side is new debt; both are described
#                     above and both carry waivers.

#
# 2026-08-20          134 -> 135. +1 from the multi-provider branch (#2001),
#                     which added ten skips and keeps ONE. Nine were eliminated
#                     rather than waived: they guarded on a model still being
#                     present in the embedded models.dev snapshot, and a snapshot
#                     refresh that drops a fixture model is a reason to repoint
#                     the test, not to report ok — so they are t.Fatal with a
#                     message naming the fixture to replace. The one kept is
#                     TestProviderCommandBuildsCLIOnly's -short guard: it shells
#                     out to `go build -tags clionly`, which is what -short is
#                     for. Permanent by design, carries a SKIP-WAIVER comment and
#                     deliberately no tracking issue, on the #1546 precedent.
#                     CI does not pass -short, so the variant is still covered.
#
# 2026-08-27          135 -> 136. +1 from the #2106 review follow-ups. The
#                     table case is `memory status` against a .memory the
#                     caller cannot enter — the exact scenario whose error
#                     message names uid 1001, and the one memoryDirError's
#                     fs.ErrPermission branch never actually reached, because
#                     os.Stat needs only +x on the parent. The fixture is a
#                     chmod 0o000 that root walks straight through, so at
#                     euid 0 the case would report ok while asserting the
#                     opposite. There is no permission-free route to EACCES:
#                     ENOTDIR and ELOOP, which no euid bypasses, land in
#                     memoryDirError's other branches and are already covered
#                     by the "not a directory" case in the same table.
#                     Permanent platform guard, not debt, so it carries a
#                     SKIP-WAIVER comment and deliberately no tracking issue,
#                     on the #1546 precedent. The other three cases in the
#                     table run as root, and the branch added no other skip.

136
