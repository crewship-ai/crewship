# Skip budget baseline — the maximum number of `t.Skip` / `t.Skipf` /
# `t.SkipNow` calls allowed in tracked *_test.go files.
#
# Enforced by scripts/skip-budget.sh, which runs in the `shell` job of
# .github/workflows/ci.yml. The gate fails when the count RISES above this
# number; lowering it is always allowed and the script nudges you to record
# the win here.
#
# To raise this number legitimately, see the failure message the script
# prints: tracking issue + `// SKIP-WAIVER(#NNNN):` comment above the skip +
# the bump in the same commit.
#
# History
#   119  2026-07-30  Initial measurement at 225bf153 (80 test files). Roughly:
#                    ~28 Docker-daemon guards, ~26 runtime.GOOS guards, ~9
#                    testing.Short guards, a handful for missing binaries and
#                    live-provider credentials. No waiver comments existed
#                    when the gate landed; these are grandfathered.
#   120  2026-07-30  +1 from #1546's durable-write regression guard
#                    (internal/memory/persona_writefailure_test.go). It injects
#                    failure via a read-only directory, which cannot deny root,
#                    so it guards on euid. Permanent platform guard, not debt —
#                    carries a SKIP-WAIVER comment but deliberately no tracking
#                    issue, because there is nothing to come back and fix.
#   121  2026-07-30  +1 from #1486's tamper-evidence bypass suite
#                    (internal/journal/verify_bypass_test.go). Real debt, and
#                    the only entry here that is: the test states an invariant
#                    that does NOT hold today — VerifyChain reports OK after a
#                    permanent→normal priority downgrade — and is skipped
#                    behind SKIP-WAIVER(#1572) so the fix has a red test to
#                    turn green. Lower this back to 120 when #1572 lands.
#   120  2026-07-30  −1: #1572 landed. The waived test is unskipped and green,
#                    the waiver comment is gone, and the debt entry above is
#                    closed out rather than left standing.
#   123  2026-08-07  +3 from #1836's composition substrate. None is debt; all
#                    three are conditional skips that report honestly instead
#                    of asserting over an empty set, and each carries a
#                    SKIP-WAIVER comment:
#                      · crewship_step_test.go — a save-time guard against a
#                        verb with no policy action. Every verb is governed, so
#                        there is nothing to refuse; it un-skips itself the day
#                        an ungoverned verb is declared.
#                      · crewship_actions_test.go — the dispatch-time twin of
#                        the same guard, skipping for the same reason.
#                      · crewship_step_test.go — runtime.Caller guard on the
#                        schema-parity test. Permanent platform guard, never
#                        taken on any platform CI runs.
#                    The branch also REMOVED two skips it had added earlier:
#                    chain_depth_hop_test.go's pair were marked BROKEN because
#                    the composition budget did not survive the journal hop.
#                    It does now (depth) and the chain no longer re-roots every
#                    hop (origin), so both are unskipped and green rather than
#                    left standing as documentation of a fixed defect.

123
