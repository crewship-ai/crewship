---
title: "crewship doctor"
description: "Pre-flight diagnostic: container runtime, port binding, DB perms, telemetry status, update availability."
icon: "stethoscope"
---

# crewship doctor

Runs a battery of checks against the local Crewship install and prints
one row per check with a status. Designed for the "is everything OK
before I file an issue?" workflow.

```bash
crewship doctor [--fix] [--json] [--no-color]
```

| Flag | Type | Default | Description |
|------|------|---------|-------------|
| `--fix` | `bool` | `false` | Attempt safe auto-repairs while running the checks. Currently creates the missing data directory; future checks may opt in. |
| `--json` | `bool` | `false` | Emit machine-readable JSON to stdout instead of the colored human table. Each check lands as a `snake_case`-keyed object — ideal for piping into `jq` or a healthcheck. |
| `--no-color` | `bool` | `false` | Disable ANSI colours. **Global root flag** (not doctor-specific), available on every command. Automatically enabled when stdout is not a TTY (no need to pass the flag in CI / log-collection scripts). |

## Exit codes

| Code | Meaning |
|------|---------|
| `0` | All checks PASS/INFO/WARN. Safe to run. WARNs indicate degraded features (read the remediation column) but do not block startup. |
| `1` | One or more checks FAIL. Server cannot start (or some critical feature is broken). |

WARN does **not** fail the command — you can wire `crewship doctor`
into a healthcheck without false alarms on transient external state
(e.g. Sentry endpoint briefly unreachable). FAIL means a core
requirement for starting Crewship is missing or broken; treat the
non-zero exit as a hard gate.

The gate applies to **both** output modes. With `--json` the complete
payload is written to stdout *before* the non-zero exit, so a CI job can
read the counters and branch on `$?` from the same invocation:

```bash
crewship doctor --format json > doctor.json   # exit 1 if anything FAILed
jq '.failed, [.checks[] | select(.status=="FAIL") | .name]' doctor.json
```

Machine formats also emit the usual structured error envelope on
**stderr** (`{"error": {"message": "N check(s) FAILED …", "exit_code": 1}}`),
matching every other command — stdout stays reserved for the payload.

<Note>
Running `crewship doctor` before the daemon is up FAILs the *server
reachable* check, and therefore exits 1. That is intended (the check
describes a real condition), but it means a CI probe on a fresh runner
should assert on the `--json` payload with an allowlist rather than on
the bare exit code — see the two smoke workflows for the pattern.
</Note>

## Check categories

Server-dependent checks (*server reachable*, *episodic recall mode*, *cli server
scheme*) resolve the same effective server other commands dial: `--server` flag →
active profile (`--profile` / `CREWSHIP_PROFILE`) → `CREWSHIP_SERVER` env → config.
An active profile that has no `server:` entry fails closed — the scheme check reports
INFO instead of silently diagnosing a different host than your API calls use.

### Container runtime

Reports the runtime `crewship start` would **actually** use, honouring
the configured provider (`container.provider`, overridable with
`CREWSHIP_CONTAINER_PROVIDER`; default `docker`). The detail column
always ends with `[provider=…]` so you can see which selection was
applied:

| `container.provider` | Selection |
|----------------------|-----------|
| `docker` (default) | Docker-compatible runtime only — Docker, Podman, Colima, OrbStack, Rancher Desktop. Apple Containers being up does **not** satisfy it. |
| `apple` | Apple Containers only. |
| `auto` | Apple Containers **first**, Docker as fallback — the same order `initProviders` uses at startup. |

- **PASS** — the selected provider answered; agents can run.
- **FAIL — `… installed but not running`** — start the runtime with the
  hint shown.
- **FAIL — `no container runtime installed`** — install one; see
  [Install — container runtime requirement](/guides/install).
- **FAIL — `pinned to docker/apple`** — a runtime is up but it is not
  the one the provider setting selects. Either start the selected one or
  repoint `CREWSHIP_CONTAINER_PROVIDER`.

<Note>
`crewship start --config <file>` can also set `container.provider` from
YAML. Doctor has no `--config` flag, so it resolves the env var + default
only — which is why the assumed provider is always printed.
</Note>

### Apple Containers (macOS only)

An informational row (**PASS**/**INFO**, never WARN/FAIL) reporting
whether the native macOS provider is available — including when Docker
answered first. Previously doctor probed Apple only as a Docker fallback
and stayed silent about it on every machine where Docker was running.

### Port binding

Binds the address `crewship start` would bind — `CREWSHIP_HOST` (default
`0.0.0.0`) on `CREWSHIP_PORT` (default `8080`) — and releases it
immediately.

- **PASS — `0.0.0.0:8080 is bindable`** — the port is free.
- **PASS — `… in use by the running crewshipd (expected)`** — the port is
  taken, but `/healthz` on it answers as crewshipd. Running doctor while
  the daemon is up is the most common invocation; that is not a failure.
- **FAIL — `… is in use by another process`** — something else holds the
  port. `lsof -i :8080` (Linux: `ss -ltnp sport = :8080`) finds the
  squatter, or set `CREWSHIP_PORT`.
- **INFO — `server … is remote`** — the configured server is not
  loopback, so the listener lives on that host and a local bind test
  would answer a question nobody asked.

### Server reachable

TCP-dials the effective server and reports **which configuration layer
chose it** — `--server flag`, `profile "dev2"`, `CREWSHIP_SERVER env`,
`config file (server:)` or `built-in default`. A forgotten
`CREWSHIP_SERVER` in one shell and a `server:` line in
`cli-config.yaml` otherwise produce byte-identical output, which is
exactly the confusion this check exists to remove.

```
[PASS]  server reachable   TCP dev2.internal:8080 ok (from profile "dev2")
```

- **PASS** — the dial succeeded.
- **WARN — a *local* server did not answer.** Expected when you have not
  run `crewship start` yet, which is the normal state when doctor is used
  as a pre-flight check. It deliberately does **not** trip the exit-1 gate,
  so `crewship doctor && crewship start` works on a healthy machine.
- **FAIL — a *remote* server did not answer**, or the URL is unparseable.
  Nobody configures a remote server by accident, so an unreachable one is
  a real fault.

### CLI / server version

Compares this binary's version against the running daemon's
(`GET /api/v1/system/version`).

- **PASS** — both builds match (a leading `v` on either side is ignored).
- **WARN** — mismatch. Run [`crewship self-update`](/cli/self-update); a
  stale CLI against a newer server is the most common source of
  confusing API errors.
- **INFO** — skipped: development build, server unreachable, or not
  logged in. Doctor never double-reports a dead daemon — the *server
  reachable* row owns that failure.

### Sidecar binary

Locates `crewship-sidecar` (in `~/.crewship/bin/` or next to the CLI)
and, when this binary was built with the sidecar hash injected at link
time, compares the file's content hash against it.

- **PASS — `<path>`** — found; no build-time hash available to compare
  against (plain `go build`, container image builds).
- **PASS — `<path> (hash abc123def456, matches this build)`** — the
  on-disk sidecar is the one this binary was built alongside.
- **WARN — `… is stale: hash X, this binary was built against Y`** — the
  deploy updated the server but not the sidecar. Rebuild and recopy
  (`make build:sidecar`), then `crewship crew restart-agents` —
  restarting alone remounts the same stale file.
- **WARN — `not found on disk`** — may be embedded in crewshipd; the
  searched paths are listed in the hint.

### Database file permissions

Stats `~/.crewship/` directory and the SQLite DB file. Expects:

- Parent directory: `0700`
- DB file: `0600`
- WAL + SHM sidecars (if present): `0600`

Drift here usually means a backup was restored via `tar` without
preserving mode bits. Fix:

```bash
chmod 0700 ~/.crewship
chmod 0600 ~/.crewship/crewship.db ~/.crewship/crewship.db-wal ~/.crewship/crewship.db-shm 2>/dev/null
```

### NEXTAUTH_SECRET

Surfaces where the JWT signing secret lives (env var vs `<dataDir>/secrets.env`) and validates the value is at least 32 characters. As of PR #446 the secret is auto-bootstrapped on first start, so a missing value is no longer a failure — doctor reports INFO and the next `crewship start` generates it.

| Result | Trigger |
|--------|---------|
| **PASS — `env-provided (N chars)`** | `NEXTAUTH_SECRET` is in the process env and ≥ 32 chars. |
| **PASS — `auto-managed in <path> (N chars)`** | Not in env; the persisted `secrets.env` has a valid entry. |
| **INFO — `not yet bootstrapped`** | Not in env and `secrets.env` doesn't exist yet. The next `crewship start` will generate and persist it. |
| **WARN — `env-provided value is short (N chars)`** | In env but below 32 chars. Regenerate with `openssl rand -hex 32`. |
| **WARN — `persisted value invalid: …`** | The persisted entry exists but failed `ValidateNextAuthSecret`. Replace the line in `secrets.env`. |
| **WARN — `persisted secret file exists but NEXTAUTH_SECRET entry is missing`** | The file was hand-edited or partial; delete it so the next `start` regenerates (any credentials encrypted under the missing key are unrecoverable). |
| **WARN — `cannot inspect persisted secret file: …`** | Usually a permission/ownership problem on the data dir. Check `~/.crewship` (or `$CREWSHIP_DATA_DIR`) permissions. |

### Episodic recall mode

Reads the `episodic` field from the running server's `/healthz`.
Reports whether [episodic memory](/guides/episodic-memory) recall runs
with a vector embedder or degraded to keyword/FTS only.

- **PASS — `vector + sparse recall`** — an embedder is configured
  (`KEEPER_OLLAMA_URL`); the boot-time indexer sweeper is embedding
  journal entries and recall serves vector + BM25 results.
- **WARN — `sparse-only`** — no embedder configured. Recall still
  works on keywords, but vector similarity is off. Set
  `KEEPER_OLLAMA_URL` to an Ollama host serving `nomic-embed-text`.
- **INFO — server not reachable / older server** — the daemon is down
  (the *server reachable* check already reports that) or predates
  the `episodic` health field.

### Legacy crew resources

Calls the authenticated `GET /api/v1/admin/legacy-resources` endpoint to detect
orphaned **pre-C1 (slug-only) crew docker resources** that survive
`crewship seed --nuke` and make every agent in the affected crew fail to start
— surfaced to users only as a generic *"failed to start agent container"*.
(Detection runs on this admin endpoint rather than the unauthenticated
`/healthz` path, so a slow docker daemon can't stall health probes.)

- **PASS — `clean`** — no orphaned legacy volumes/containers for any
  current crew slug.
- **WARN — `present`** — at least one orphaned pre-C1 resource exists.
  Agents in the affected crew(s) will fail to start. Run
  [`crewship admin prune-legacy`](/cli/admin) to remove them.
- **INFO — not logged in / unreachable / non-docker server** — the check needs
  an authenticated session (`crewship login`); or the daemon is down (the
  *server reachable* check covers that) or the container provider isn't docker.

### Telemetry status

Reads `app_settings.telemetry_opt_in` from the local DB. Reports:

- **PASS — ENABLED + DSN** — telemetry is on and a DSN is wired in.
  Shows the endpoint host (vendor default vs `CREWSHIP_SENTRY_DSN`
  override).
- **PASS — DISABLED** — operator opted out via `crewship telemetry off`.
- **WARN — ENABLED but no DSN** — consent recorded but no DSN
  available. Local dev builds without ldflag injection; not an error,
  just informational.
- **WARN — not configured** — fresh DB, never asked. Will default to
  ENABLED on the next `crewship start` (v0.1 beta behaviour).

See [Telemetry](/guides/telemetry) for opt-out and routing-override
recipes.

### DSN reachability

Only runs when telemetry is enabled AND a DSN is set. Best-effort TCP
connect (5s timeout) to the resolved endpoint at port 443.

- **PASS** — endpoint reachable; events will ship.
- **WARN** — endpoint unreachable. Crashes won't ship until network
  heals. Not a Crewship health signal — could be local firewall, DNS
  issue, or Sentry outage. `crewship doctor` does **not** fail on
  this; you'd be silently angry at the wrong layer.

### Update available

Calls the GitHub Releases API for the project (cached 24h locally).

- **PASS** — running latest stable, or running a newer pre-release.
- **WARN** — newer stable available; output shows current → latest
  with install hint (`brew upgrade crewship` or `docker pull`).
- **INFO** — running a dev build, version check skipped.

Suppress with `CREWSHIP_SKIP_UPDATE_CHECK=1`.

## Example output

PR #441 added a version + OS/arch header banner and a "Next steps" footer that points the operator at the three commands that actually move them forward (or at the troubleshooting page on FAIL). Presentation-only — no check behaviour changes.

```
$ crewship doctor

  Crewship doctor — environment check
  binary:  v0.1.0-beta.4   os: darwin/arm64

  [PASS]  container runtime                 docker 27.3.1 (/var/run/docker.sock)  [provider=docker]
  [INFO]  apple containers                  not available (optional native macOS provider)
  [PASS]  port binding                      0.0.0.0:8080 in use by the running crewshipd (expected)
  [PASS]  data directory                    /Users/you/.crewship
  [PASS]  data dir writable                 touch test ok
  [PASS]  db migration version              v85 (latest)
  [PASS]  sidecar binary                    /Users/you/.crewship/bin/crewship-sidecar (hash 3f9a12c40b7e, matches this build)
  [PASS]  NEXTAUTH_SECRET                   auto-managed in /Users/you/.crewship/secrets.env (64 chars)
  [PASS]  server reachable                  TCP localhost:8080 ok (from config file (server:))
  [PASS]  cli/server version                both on v0.1.0-beta.4
  [PASS]  episodic recall mode              vector + sparse recall (embedder configured, indexer running)
  [PASS]  legacy crew resources             no orphaned pre-C1 crew volumes/containers
  [PASS]  telemetry status                  enabled, endpoint o4511387688108032.ingest.de.sentry.io
  [PASS]  sentry DSN wiring                 DSN resolved (o4511387688108032.ingest.de.sentry.io)
  [PASS]  telemetry endpoint                TCP o4511387688108032.ingest.de.sentry.io:443 ok
  [PASS]  data dir perms                    dir 0700, db file 0600 (/Users/you/.crewship)
  [WARN]  update check                      v0.1.0 available, you're on v0.1.0-beta.4
           brew upgrade crewship  OR  docker pull ghcr.io/crewship-ai/crewship:latest
  [PASS]  cli server scheme                 localhost:8080 (http on loopback — fine)
  [PASS]  cli config perms                  /Users/you/.crewship/cli-config.yaml mode 0600
  [PASS]  local-model endpoint              ollama-local reachable

  1 warned — review and re-run.

  Next steps:
    1. crewship start
    2. open http://localhost:8080
    3. follow the onboarding wizard (workspace → crew → agent → credentials)
```

The *apple containers* row only appears on macOS. On a FAIL run the
footer is replaced with `N failed, M warned — fix the FAILs and re-run.`
plus the troubleshooting link, and the command exits 1.

## Output formats

- `--no-color` — disable ANSI colors. Auto-detected when stdout is
  not a TTY (so log-collection scripts get plain text without any
  flag).
- Status codes (`PASS`/`WARN`/`FAIL`/`INFO`) are always in left-padded
  brackets and parseable with `awk '{print $1}'`.

## Auto-repair (`--fix`)

`--fix` opts into safe, narrowly-scoped repair side-effects during the
check run. Currently scoped to:

- **Database directory missing** — creates `$CREWSHIP_DATA_DIR` (or
  `~/.crewship`) with `0700` perms instead of failing the check. The
  detail column then reads `(created via --fix)` so you can see what
  changed.

Anything destructive (file overwrites, mode-bit flips on existing
files, DB schema repairs) is intentionally out of scope — `--fix` is
for first-run onboarding, not running surgery on a populated install.

## When to run

- **Before opening a GitHub issue** — attach `crewship doctor
  --no-color` output. Saves a back-and-forth diagnostic round.
- **After upgrading the binary** — pre-migration snapshot has run by
  this point; doctor confirms the new binary is happy with the
  existing DB.
- **In CI smoke tests** — the `.github/workflows/smoke-test.yml` and
  `nightly-smoke.yml` workflows run `crewship doctor` against the
  freshly-built binary to catch broken binaries before they reach users.
  Both parse `--format json` and require that every FAIL is in a small
  allowlist (`container runtime`, on the Docker-less macOS runner), so a
  genuine regression in any other check still fails the job. Copy that
  shape rather than `|| true`-ing the exit code.

## Related

- [`crewship start`](/cli/start) — the command doctor is checking
  for. If doctor is green, `start` should succeed.
- [`crewship telemetry`](/cli/telemetry) — flip consent state that
  doctor reports.
- [`crewship version`](/cli/version) — same version info doctor uses
  for the update-availability check.
- [Troubleshooting](/guides/troubleshooting) — symptom-keyed catalog
  of common failures.
