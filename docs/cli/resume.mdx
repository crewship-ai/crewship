---
title: "Resume"
description: "Pick up an existing session by chat-id, run-id, or PR URL — or pick interactively from the 10 most recent CLI sessions."
icon: "play"
stability: experimental
tag: Experimental
---

# Resume

`cmd/crewship/cmd_resume.go` continues a previous session by threading `--chat <id>` into the standard `run` flow against the agent that owns the chat. Four resolution paths:

| Input | Resolution |
|---|---|
| (no arg) | Interactive `huh` picker over the 10 most recently active sessions, read from `GET /api/v1/runs` and deduplicated by chat. Requires a TTY on stdin **and** stderr. |
| `c_…` | Treated as a chat ID. |
| `r_…` / `run_…` | Looked up via `client.GetRun`; the run's `chat_id` is followed. |
| `https://github.com/owner/repo/pull/<n>` | Searches the journal for the PR identifier and reuses the chat that produced it (best-effort — only works if an agent journaled the PR creation). |

If the agent slug isn't part of the resolved object, `resume` finds the agent that owns the chat by walking `GET /api/v1/agents` and reading each agent's `GET /api/v1/agents/{agentId}/chats`. A chat is only addressable under its agent — there is no flat `GET /api/v1/chats/{chatId}` — so this walk is the lookup.

<Note>
Before [#2086](https://github.com/crewship-ai/crewship/issues/2086), both resolution paths called routes that do not exist (`GET /api/v1/chats` for the picker, `GET /api/v1/chats/{chatId}` for the agent lookup). The picker silently fell through to its `/runs` fallback; the agent lookup just failed, so `crewship resume <chat-id>` always reported `could not determine agent`.
</Note>

## `crewship resume [chat-id | run-id | pr-url]`

No flags of its own — all flags belong to `crewship run` (it's the dispatch target). The two flags that get forced are `--chat <id>` and `--interactive=true`.

### Examples

```bash
crewship resume                                          # interactive picker
crewship resume c_abc123                                 # explicit chat
crewship resume r_xyz789                                 # via run
crewship resume https://github.com/foo/bar/pull/42       # via journal lookup
```

### Common errors

- **`interactive picker requires a TTY; pass a chat-id or run-id`** — running without a TTY but no argument.
- **`run <id> has no associated chat to resume`** — the run pre-dates chat threading or was created without one.
- **`could not determine agent for chat <id>: …`** — no agent in the workspace lists that chat (wrong workspace, or the chat id is a typo). The wrapped error names what the lookup actually hit.
- **`no recent sessions to resume`** — no run in the workspace's recent history carries a chat id.
- **`no session found for PR <owner/repo#n> — pass a chat-id directly`** — journal search came up empty; fall back to the chat-id form.
- **`list recent sessions: <err>`** — the runs list the picker reads failed.

## See also

- [`crewship history`](/cli/history) — list of recent runs, grab a chat-id from the table.
- [`crewship recap <chat-id>`](/cli/recap) — summarise the session instead of continuing it.
- [`crewship retry <run-id>`](/cli/retry) — start a fresh run with the same prompt.
