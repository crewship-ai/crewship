---
title: "Open"
description: "Open the web-UI page for a CLI resource in your default browser. Hybrid terminal/web workflow."
icon: "external-link"
stability: experimental
tag: Experimental
---

# Open

`cmd/crewship/cmd_open.go` opens the relevant web-UI page for a CLI resource in your default browser. The point: do everything in the terminal until you hit something better viewed visually (a chat, a mission timeline, an approval queue), then jump.

`open` deliberately does **not** require auth — it's just a URL resolver. The browser handles login if needed.

Cross-platform launcher: `open` (macOS), `cmd /c start ""` (Windows), `xdg-open` (Linux). Dependency-free — uses `os/exec` rather than a Go browser library.

## `crewship open <resource> [id]`

| Flag | Type | Default | Effect |
|---|---|---|---|
| `--print-only` | bool | `false` | Print the URL to stdout instead of launching the browser. Useful for scripting. |

### Resources

| Resource | Args | URL path |
|---|---|---|
| `inbox` (`dashboard`, `home`) | — | `/inbox` |
| `activity` | — | `/activity` |
| `agents` | — | `/agents` (redirects to `/crews`) |
| `agent` | `<agent-slug>` | `/crews?agent=<slug>` |
| `crews` | — | `/crews` |
| `crew` | `<slug-or-id>` | `/crews/<id>` |
| `chat` | `<agent-slug>` | `/chat/<slug>` |
| `mission` | `<mission-id>` | `/missions/<id>/timeline` |
| `journal` | — | `/journal` |
| `approvals` | — | `/approvals` |
| `integrations` | — | `/integrations` |
| `routines` | — | `/routines` |
| `issues` | `[id]` | `/issues` or `/issues/<id>` |
| `runs` | — | `/runs` |
| `settings` | — | `/settings` |
| `admin` | — | `/admin` |
| `audit` | — | `/audit` |
| `credentials` | — | `/credentials` |

IDs that land in a path segment are `url.PathEscape`'d, so slugs with `:` or other reserved characters survive. `agent` puts its argument in a query value instead and uses `url.QueryEscape` — `PathEscape` leaves `&` and `=` alone, since both are legal in a path segment, and either would split the query string.

<Note>
**`agent` takes a slug, not an id.** There is no agent detail page: the route was folded into the `/crews` canvas, which selects an agent from `?agent=<slug>` and ignores anything that is not one (`hooks/use-crews-selection.tsx`). Pass an id and you get the roster with nothing selected — `open` resolves URLs offline and without auth, so it has no way to translate one. `crewship agent get <slug-or-id>` prints the slug.

`agent` used to build `/agents/<id>`, which matches no route and fell through to the SPA root — the dashboard, under a URL that said agents.
</Note>

### Examples

```bash
crewship open inbox
crewship open chat viktor
crewship open mission MIS-42
crewship open --print-only mission MIS-42       # print URL, don't open
crewship open agent viktor                      # /crews with Viktor selected
crewship open issues CRE-128
```

### Common errors

- **`unknown resource "foobar" (try: inbox, activity, agent[s], crew[s], chat, mission, journal, approvals, integrations, routines, issues, runs, settings, admin, audit, credentials)`** — typo or unsupported resource.
- **`<resource> requires exactly 1 argument(s), got 0`** — missed the ID.

## See also

- [`crewship inbox`](/cli/inbox) — the same destination, but stays in the terminal.
- [`crewship login`](/cli/login) — the `--server` flag this command honours. The browser
  URL uses the effective server (`--server` → active profile → `CREWSHIP_SERVER` env →
  config), so it opens the same instance other commands dial.
