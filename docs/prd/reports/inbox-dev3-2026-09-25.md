# Inbox clarity — dev3 handoff (2026-09-25)

This is the Inbox increment in draft PR #2697 on `feat/dashboard-live-work`.
It continues the dashboard clarity work tracked by #2696.

## Current Inbox

- Needs action places requests for a human decision before routine alerts.
  The right overview separates one waiting decision from three routine alerts
  in the current dev3 workspace; updates remain below them.
- The left list is wider on desktop. Schedule and paused routine notices use
  their status colours; history navigation stays muted.
- Dashboard links with `?attention=` filter both the left list and right pane.
  The decision link includes human-input runs and source-backed approvals;
  source-less system advisories are excluded. The focused pane shows the
  matching entries and an All inbox link.
- The desktop main pane previews the first visible item without marking it
  read. Explicit selection and mobile navigation still open the full detail.
  The left crew picker uses crew icons, colours, searchable names and slugs,
  and sorts crews with matching items first.
- Repeated system advisories remain grouped. Their underlying item count is
  shown in the focused summary, and opening a group resolves to its detail.
  Returning from detail preserves the selected attention filter.

## Deployment and verification

Dev3 is `https://crewship-dev3.unifylab.cz/inbox`, service `crewship-ws@3`.
The installed binary was built from local deploy branch
`deploy/dev3-dashboard-20260925` at `e7782b896`, corresponding to PR commits
`07c75e9a7`, `a9c9890c8`, and `56a62147b`. The preceding binary is backed up at
`/srv/crewship/dev3-pages-release/backups/crewship.unsigned.pre-inbox-focus-20260925`.
The sidecar binary was not replaced.

- All 94 focused Inbox and dashboard tests, frontend typecheck, lint, build,
  Go Inbox aggregate tests, Go vet and diff whitespace check passed. The
  deploy branch static export and Go binary build passed.
- Local and public URLs returned HTTP 200; systemd reported active.
- Authenticated Chromium at 1440 px saw the decision first and the separate
  routine alert list. Dashboard shows one actual decision, and its link opens
  the focused pane with that decision. The earlier 13 count included 12
  system advisories.
- Authenticated Chromium at 390 px found no horizontal overflow. The browser
  reported no page errors in these checks.

The earlier raw approval category contained 12 grouped system advisories and
one actual human decision in this demo workspace. The new `decision_count`
aggregate uses the same source-action rules as Inbox and counts the one
decision. It is computed in the existing active-kind query, without storing
new rows or asking the browser to load the entire feed.

Keep the PR draft until an actual CodeRabbit review is posted; a passing
rate-limited check is not a review.
